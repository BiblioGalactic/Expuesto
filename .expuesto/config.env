#!/usr/bin/env bash
# ============================================================
# 🔧 EXPUESTO — Configuración Centralizada
# ============================================================
# Fuente única de verdad para rutas y parámetros globales.
# Uso: source "${EXPUESTO_ROOT}/.expuesto/config.env"
# ============================================================

# === 📍 Rutas base ===
EXPUESTO_ROOT="${EXPUESTO_ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
LLAMA_CLI="${LLAMA_CLI:-$HOME/modelo/llama.cpp/build/bin/llama-cli}"
MODELS_DIR="${MODELS_DIR:-$HOME/modelo/modelos_grandes}"
MODELO="${MODELO:-${MODELS_DIR}/M6/mistral-7b-instruct-v0.1.Q6_K.gguf}"

# === 🧠 Parámetros por defecto de llama-cli ===
LLAMA_CTX_SIZE="${LLAMA_CTX_SIZE:-4096}"
LLAMA_THREADS="${LLAMA_THREADS:-6}"
LLAMA_TEMP="${LLAMA_TEMP:-0.8}"
LLAMA_N_PREDICT="${LLAMA_N_PREDICT:-512}"

# === 📁 Directorios de trabajo ===
EXPUESTO_LOG_DIR="${EXPUESTO_LOG_DIR:-${EXPUESTO_ROOT}/.expuesto/logs}"
EXPUESTO_TMP_DIR="${EXPUESTO_TMP_DIR:-${TMPDIR:-/tmp}/expuesto-$$}"
EXPUESTO_LIB_DIR="${EXPUESTO_ROOT}/lib"

# === 📊 Logging ===
LOG_MAX_LINES="${LOG_MAX_LINES:-10000}"
LOG_ROTATE_COUNT="${LOG_ROTATE_COUNT:-5}"

# === 🔐 Seguridad ===
EXPUESTO_CONTACT_EMAIL="gsilvadacosta0@gmail.com"

# === ✅ Crear directorios si no existen ===
mkdir -p "$EXPUESTO_LOG_DIR" 2>/dev/null || true
